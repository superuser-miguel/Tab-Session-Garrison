.sessionItem {
  position: relative;
  background-color: var(--main-bg);

  // Skip layout/paint for rows scrolled out of view on large profiles, while
  // keeping every row in the DOM (so flexbox `order` sorting, keyboard nav, and
  // the ref-based scroll-to-selected all still work). `auto` remembers each
  // row's real height once rendered; the fallback covers not-yet-seen rows.
  content-visibility: auto;
  contain-intrinsic-size: auto 52px;

  .selectButton {
    border: none;
    border-bottom: 1px solid var(--line);
    padding: 8px 5px;
    width: 100%;
    text-align: left;
    background-color: rgba(0, 0, 0, 0);
    cursor: pointer;

    .name {
      display: block;
      color: var(--main-text);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: normal;

      &.isTruncate {
        white-space: nowrap;
      }
    }

    .lineContainer {
      display: flex;
      justify-content: space-between;
      align-items: center;

      .windowsInfo,
      .date {
        font-size: 11px;
        color: var(--sub-text);
        overflow: hidden;
        white-space: nowrap;
      }

      // The count can truncate (…) when cramped; the date must never break.
      .windowsInfo {
        min-width: 0;
        text-overflow: ellipsis;
      }

      .date {
        flex-shrink: 0;
      }

      .groupDots {
        display: inline-flex;
        align-items: center;
        gap: 3px;
        margin-left: 6px;
        margin-right: auto;
        flex-shrink: 0;

        .groupDot {
          width: 8px;
          height: 8px;
          border-radius: 50%;
        }

        .groupOverflow {
          font-size: 10px;
          color: var(--sub-text);
          line-height: 1;
          margin-left: 1px;
        }
      }
    }

    .tagsContainer {
      display: flex;
      flex-direction: row;
      width: 100%;
      flex-wrap: wrap;
      justify-content: flex-start;
      align-items: flex-start;
      overflow: hidden;

      margin: 0;
      padding: 0;
      list-style-type: none;
      .tag {
        .tagIcon,
        .autoSaveIcon,
        .startupIcon,
        .trackingIcon,
        .winCloseIcon,
        .browserExitIcon,
        .manualSaveIcon {
          width: 11px;
          margin-right: 3px;
          fill: var(--sub-text);
        }
        .autoSaveIcon {
          width: 16px;
        }
        .winCloseIcon,
        .browserExitIcon {
          width: 13px;
        }
        .manualSaveIcon {
          width: 11px;
        }
        .startupIcon {
          width: 14px;
        }
        .trackingIcon {
          width: 14px;
        }
        span {
          text-overflow: ellipsis;
          white-space: nowrap;
          overflow: hidden;
        }
        display: flex;
        align-items: center;
        justify-content: space-between;
        min-width: 30px;
        font-size: 11px;
        height: 14px;
        color: var(--sub-text);
        background-color: var(--line);
        border-radius: 10px;
        padding: 3px 8px;
        margin-bottom: 2px;
        margin-right: 2px;
        overflow: hidden;

        &.tagRegular {
          color: var(--tag-regular);
          background-color: var(--tag-regular-bg);
          svg {
            fill: var(--tag-regular);
          }
        }
        &.tagWinClose {
          color: var(--tag-winclose);
          background-color: var(--tag-winclose-bg);
          svg {
            fill: var(--tag-winclose);
          }
        }
        &.tagBrowserExit {
          color: var(--tag-browserexit);
          background-color: var(--tag-browserexit-bg);
          svg {
            fill: var(--tag-browserexit);
          }
        }
        &.tagManual {
          color: var(--tag-manual);
          background-color: var(--tag-manual-bg);
          svg {
            fill: var(--tag-manual);
          }
        }
      }
    }
  }

  &.isSelected,
  &.isMultiSelected {
    background-color: var(--highlight-bg);
    .selectButton {
      box-shadow: inset 3px 0 0 var(--highlight);
    }
  }

  &:not(.isSelected):not(.isMultiSelected):hover {
    background-color: var(--line);
    .tag:not(.tagRegular):not(.tagWinClose):not(.tagBrowserExit):not(.tagManual) {
      background-color: var(--line2);
    }
  }

  &.isTracking .tagsContainer .tag .trackingIcon {
    fill: var(--error);
  }

  .buttonsContainer {
    position: absolute;
    opacity: 0;
    transform: translateX(10px);
    background-color: rgba(255, 255, 255, 0.8);
    height: 100%;
    right: 0;
    bottom: 0;
    display: flex;
    flex-direction: row;
    flex-shrink: 1;
    transition: all 200ms;

    button {
      display: flex;
      align-items: center;
      padding: 13px;
      border: none;
      background-color: transparent;
      cursor: pointer;
      transition: all 200ms;

      svg {
        width: 13px;
        fill: var(--highlight);
      }

      &:hover {
        background-color: rgba(0, 0, 0, 0.2);
      }

      &.remove {
        margin-left: 5px;

        &:hover svg {
          fill: var(--confirm);
        }
      }
    }
  }

  &:hover .buttonsContainer {
    opacity: 1;
    transform: translateX(0);
    transition: all 300ms;
  }
}
