node_modules/
dist
dev
temp
web-ext-artifacts
*.xpi
src/credentials.js*~

# Internal only: WIP visual / UI idea screenshots. Never committed to the
# (public) repo, never shipped in the extension, never in the source archive.
ideas/
